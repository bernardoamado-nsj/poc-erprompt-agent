# POC ERPrompt Agent

POC de um sistema que usa IA para gerar telas dinamicamente.

## Setup do ambiente

Inicie o servidor do backend:
> npm run agent:serve

Inicie o servidor do frontend:
> npm run dev